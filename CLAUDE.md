# このリポジトリでの作業ルール

## タスク完了の定義（恒久ルール）

タスクを完了したとみなすには、以下の3点すべてを実行すること。いずれか1つでも欠けている状態を「完了」と報告してはならない。

1. 作業内容を `docs/reports/{日付}_{内容}.md` として保存する
2. 変更内容を git commit し、リモートへ push する
3. `scripts/notion_report.js` を使い、報告テンプレート（下記）に沿った構造でNotionへ投稿する

区切りの良い単位ごとにコミットし、完了時にpushする。

## Notion報告テンプレート（構造厳守）

`scripts/notion_report.js` が自動的にこの構造でブロックを組み立てる。報告JSONの詳細は [README.md](./README.md) を参照。

1. callout（📋・青背景）: 3行以内の要約
2. 見出し「結果」+ table（項目｜状態）
3. 見出し「要判断」+ callout（⚠️・赤背景）: 人間の決裁が必要な事項。無ければ「なし」
4. 見出し「未確認の範囲」+ 箇条書き
5. 見出し「禁止事項の遵守状況」+ 箇条書き
6. toggle「詳細ログ」: 長い技術詳細・コマンド出力はすべてこの中に格納する
7. divider + 見出し「次タスクへの引き継ぎ」

## 秘密情報の取り扱い

- `NOTION_TOKEN`（Notion Integration Token）等の秘密情報は、リポジトリ内のいかなるファイルにも書き込まない・コミットしない・報告本文や標準出力に出力しない。
- `NOTION_TOKEN` の値そのものは、AIエージェントとの会話にも入力させない。設定は利用者自身が `~/.bashrc`（WSL/Linux）または `~/.zshrc`（Mac）へ直接追記して行う。
- 報告文面に秘密情報を含めそうになった場合は、独自判断で伏せ字にする等せず、作業を停止してその旨を報告する。
- `.gitignore` で `.env` 系ファイルを除外している。新たに秘密情報を扱うファイルを追加する場合は、必ず `.gitignore` に追記すること。

## エラー時の方針

Notion投稿やスクリプト実行でエラーが発生した場合、独自判断で回避（リトライの握りつぶし、機能の無効化等）せず、エラー全文をそのまま報告して停止する。
