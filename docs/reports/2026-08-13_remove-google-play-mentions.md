# 2026-08-13 Day72 Google Play への言及の削除（判断結果の反映）

Day71 で報告した判断待ちの3箇所について、いずれも削除する旨の判断を受けたため反映した。
プライバシーポリシー本文への変更は、この3箇所のうち1箇所に限り明示の許可を得ている。

## 変更内容（変更前後の全文）

### 1. tokushoho.html「お支払い方法」

変更前
```html
      <tr>
        <th>お支払い方法</th>
        <td>App Store（Apple）または Google Play（Google）の決済システムによります。</td>
      </tr>
```

変更後
```html
      <tr>
        <th>お支払い方法</th>
        <td>App Store（Apple）の決済システムによります。</td>
      </tr>
```

### 2. tokushoho.html「お支払い時期」

変更前
```html
      <tr>
        <th>お支払い時期</th>
        <td>App Store / Google Playの規約に準じます。</td>
      </tr>
```

変更後
```html
      <tr>
        <th>お支払い時期</th>
        <td>App Store の規約に準じます。</td>
      </tr>
```

### 3. privacy.html「6. 課金について」

変更前
```html
  <h2>6. 課金について</h2>
  <p>
    本アプリ内で課金機能を提供する場合、決済処理はApple（App Store）またはGoogle（Google Play）の課金システムを通じて行われます。
    クレジットカード情報等の決済情報を本アプリまたは開発者が直接取得・保存することはありません。
  </p>
```

変更後
```html
  <h2>6. 課金について</h2>
  <p>
    本アプリ内で課金機能を提供する場合、決済処理はApple（App Store）の課金システムを通じて行われます。
    クレジットカード情報等の決済情報を本アプリまたは開発者が直接取得・保存することはありません。
  </p>
```

指示文には差し替え後の文面が「決済処理はApple（App Store）の課金システムを通じて行われます。」と
示されていたが、これは1つの文の後半である。文頭の「本アプリ内で課金機能を提供する場合、」を落とすと
Google Play の削除を超えて本文を変えてしまうため、この部分は残した。
Day71 の報告で提示し、承認を受けた想定文面とも一致している。
同じ段落の2文目（クレジットカード情報に関する記述）も変更していない。

## 変更していないもの

- 上記3箇所以外のすべて（`index.html` / `terms.html` / `support.html` は無変更）
- `assets/css/style.css` のバッジのスタイル定義（指示どおり残置。`.hero .badge` が204行目に残っている）
- デザイン・配色・各ページのURL

## 残存確認

手元のファイル、公開URLの双方で、「Google Play」「Google」「Android」「アンドロイド」「Play ストア」を
走査した結果は以下のとおり。

| ページ | HTTPステータス | 該当件数 |
|---|---|---|
| `/` | 200 | 0件 |
| `/terms.html` | 200 | 0件 |
| `/privacy.html` | 200 | 0件 |
| `/support.html` | 200 | 0件 |
| `/tokushoho.html` | 200 | 0件 |
| **合計** | — | **0件** |

`_config.yml` にも該当は無い。これでサイト全体から Google Play / Android への言及が消え、
iOS のみの提供であるという実態と一致した状態になった。

## 公開確認

`https://satamainuser.github.io/tide-app-site/` にデプロイ済み。公開URLから取得した該当箇所の実際の文面は
以下のとおりで、意図どおり反映されている。

```
<th>お支払い方法</th>
<td>App Store（Apple）の決済システムによります。</td>

<th>お支払い時期</th>
<td>App Store の規約に準じます。</td>

<h2>6. 課金について</h2>
<p>
  本アプリ内で課金機能を提供する場合、決済処理はApple（App Store）の課金システムを通じて行われます。
  クレジットカード情報等の決済情報を本アプリまたは開発者が直接取得・保存することはありません。
</p>
```

## 要判断

なし。

## 未確認の範囲

- ブラウザで実際に開いた際の見た目。公開HTMLの取得による確認にとどまる
- App Store の審査側が今回の一連の変更（Day70〜Day72）をどう評価するか
- App Store の配信開始後に、トップページのダウンロード欄へ実際のリンクを追加する作業は未実施

## 禁止事項の遵守状況

- 指示された3箇所のみを変更し、それ以外のファイル・箇所には手を入れていない
- プライバシーポリシー本文の変更は、明示の許可を得た1箇所に限っている
- バッジのスタイル定義は指示どおり残置した
- デザイン・配色・各ページのURLを変更していない
- 推測で文言を作っていない。指示文が文の後半のみを示していた箇所は、独自に文頭を削らず、
  Google Play への言及のみを削除する形にした
- 秘密情報をファイル・コミット・報告本文のいずれにも記載していない
